specVersion: "v1beta"
serviceId: "corlog"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "corlog-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
